# Marks `pages/` as a Python package so Page Objects can be imported as
# `from pages.home_page import HomePage` (Phase 3), and so mypy treats this
# directory as type-checkable code. Intentionally empty.
